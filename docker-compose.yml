version: '3.8'

services:
  postgres:
    image: postgres:18.3
    container_name: taskforge-postgres
    restart: always
    environment:
      POSTGRES_USER: taskforge
      POSTGRES_PASSWORD: taskforge_dev1
      POSTGRES_DB: taskforge
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: